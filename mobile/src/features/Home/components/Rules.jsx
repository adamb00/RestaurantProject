import React from 'react';
import PropTypes from 'prop-types';
import { SafeAreaView, StyleSheet, Text } from 'react-native';

const Rules = () => {
   return (
      <SafeAreaView>
         <Text>Here comes any rules, like GDPR</Text>
      </SafeAreaView>
   );
};

Rules.propTypes = {};
const styles = StyleSheet.create({});
export default Rules;
